import { useQuery } from 'react-query';
import api from '../../api';
import querykey from './keys';
import { UsersType } from '../users/types';
import config from '../../../config';
import mock from '../users';
